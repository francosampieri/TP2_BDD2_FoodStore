# DUIA TP4 — FoodStore

| Herramienta | Para qué se usó | Prompt/spec resumen | Qué se aceptó | Qué se corrigió o descartó y por qué |
|---|---|---|---|---|
| Kiro | Redactar `spec_consultas_tp4.md` (Spec A y Spec B) | Especificar el ranking de productos por facturación dentro de su categoría con `RANK`, y los pedidos cuyo total supera el promedio personal con subconsulta correlacionada | El spec completo, incluyendo filtros de `eliminado`, columnas de salida, ventana, orden final y criterios de equivalencia | Nada que corregir |
| OpenCode | Parte 1: analizar los `EXPLAIN ANALYZE` reales y proponer reescrituras | Proponer una reescritura por consulta basada en el plan real (facturación y ranking), un índice solo si el plan lo justificara y el nodo esperado a modificar como hipótesis | Reescritura de facturación (agrupar por `id_categoria` antes de unir `categoria`) y reescritura de ranking (pre-agregar `detalle_pedido` por `id_pedido`, eliminando `COUNT(DISTINCT)`); no se agregaron índices porque no había filtros selectivos | Ambas reescrituras **descartadas** tras la medición real: facturación 1089,669 ms vs. 946,611 ms (el `Partial HashAggregate` en memoria empeoró el tiempo); ranking 2599,644 ms vs. 1283,410 ms (los `HashAggregate` volcaron a disco: 41 batches/15.832 kB por pedido y 5 batches/11.080 kB por usuario) |
| OpenCode | Parte 2: lectura nodo a nodo del plan de facturación (946,611 ms) | Explicar el plan en 7–9 afirmaciones numeradas: joins y algoritmo, lado externo/interno, Parallel Hash Join, external merge en disco, estimación vs. real, workers y loops; sin ejecutar SQL ni proponer índices | Afirmaciones 1 a 7 y 9 | Afirmación 8 **rechazada** en su última oración: no existe un `Hash` simple sobre `pedido`; `pedido` se lee con `Parallel Seq Scan` y construye un `Parallel Hash` usado por el `Parallel Hash Join`, mientras que los `Hash` simples corresponden a `producto` y `categoria`. Además se corrigió el typo `factuado` → `facturado` |
| OpenCode | Parte 3: generar `sql/consultas_parte3_tp4.sql` | Implementar las consultas de Spec A (ranking con `RANK`) y Spec B (correlacionada por `id_usuario`), una alternativa semánticamente equivalente por cada una y dos verificaciones `EXCEPT` por sentido comparando todas las columnas; sin DDL, sin `SELECT *`, sin inventar tablas ni columnas | Consultas, alternativas y las 4 verificaciones `EXCEPT`; los filtros de `eliminado` respetan exactamente cada spec | Nada que corregir; se respetó el pedido de no afirmar ejecuciones en el script |
| OpenCode | Parte 4: competencia — consulta común de la cátedra | Adaptar la consulta común de la cátedra al esquema FoodStore en `sql/competencia_tp4.sql`, reutilizando el índice `idx_producto_cat_precio_disp_vig` ya creado y aceptado en TP3, y documentar la evidencia real en `informe_tp4.md` | Se reutilizó la solución validada del TP3 con evidencia real previa (antes 93,394 ms; después 10,855 ms; ~8.6x); el script no incluye `CREATE INDEX` ni DDL y no re-afirma ejecución | No se aplicó ningún cambio nuevo a ciegas: la mejora ya estaba probada por medición real en TP3; la reutilización de la consulta común del TP3 respondió a la indicación explícita del profesor, que prevalece sobre el ejemplo general de consulta analítica con JOIN y agregación de la guía |

**Validación manual (no IA):** las cuatro verificaciones `EXCEPT` de la Parte 3, comparando todas las columnas de salida en ambos sentidos, fueron ejecutadas manualmente en DBeaver sobre `foodstore_tp3` y devolvieron 0 filas en las cuatro direcciones (A.1 = 0, A.2 = 0, B.1 = 0, B.2 = 0).