# DUIA TP5 — FoodStore

| Herramienta | Para qué se usó | Prompt/spec resumen | Qué se aceptó | Qué se corrigió o descartó y por qué |
|---|---|---|---|---|
| Kiro | Especificar la Parte A del TP5 en `specs/plan_indexado_tp5.md` | Seleccionar consultas sobre `pedido`/`detalle_pedido` con predicado selectivo sin índice que lo cubra, definir criterios de aceptación con `EXPLAIN ANALYZE` y descartar las propuestas sin selectividad | El criterio de selección de consultas y los objetivos de medición | Con posterioridad, la consulta de pedidos cancelados quedó definida con filtro exacto `estado = 'CANCELADO'` (sin rango de fechas) y el índice parcial correspondiente incorporó `estado = 'CANCELADO'` en su condición |
| OpenCode | Proponer e implementar los índices en `sql/indices_tp5.sql` | Generar tres `CREATE INDEX` parciales y selectivos (pedido cancelado, producto no disponible, detalle de cantidad >= 5) con consulta objetivo, plan previo y expectativa a validar; sin DDL extra | Los tres `CREATE INDEX` fueron creados y validados con `EXPLAIN ANALYZE` real por la medición manual; los tres quedaron aceptados por mejora real del Execution Time | Se descartaron: el índice simple sobre `pedido (eliminado)` (baja cardinalidad), los índices para agregaciones globales sin filtro selectivo (no evitan recorrer las tablas) y el índice general sobre `pedido (total)` para "total mayor al promedio" (88.369 de 200.021 pedidos: no selectivo) |
| OpenCode | Documentar las mediciones en `informe_mediciones_tp5.md` | Registrar mediciones de lectura antes/después (210.677→2.822 ms, 31.581→2.767 ms, 182.874→1.498 ms), la medición de escritura (`foodstore_tp3` 989.437 ms vs. `foodstore_tp5` 325.245 ms) y las propuestas descartadas | La estructura del informe y la separación entre lecturas, escritura y descartes | Se aclaró que el resultado de escritura es aislado y no demuestra que los índices reduzcan el costo de inserción: la diferencia puede deberse a caché y variación de ejecución, y los triggers, las FK y el recálculo de totales tuvieron un peso relevante |
| OpenCode | Generar las vistas de la Parte B en `sql/views_tp5.sql` y sus verificaciones en `sql/test_vistas_tp5.sql` | Crear tres `CREATE VIEW` nuevos (catálogo por categoría, pedidos por usuario, detalle de pedido con histórico) respetando columnas, joins y filtros de vigencia de `specs/vistas_tp5.md`, sin `DROP VIEW` ni `CREATE OR REPLACE`; seis verificaciones EXCEPT de lectura (vista vs. consulta manual) y consulta de control de histórico | Las tres vistas y las verificaciones quedaron aceptadas; sin modificar ni reemplazar las vistas existentes de `Objects.sql` | Los comentarios de la consulta de control V3.3 se ajustaron para dejarla como control informativo (0 filas indica ausencia de casos en la base, no un error de la vista) |
| Kiro | Especificar la Parte C en `specs/vista_materializada_tp5.md` | Justificar la materialización con el antecedente del reporte de facturación (946.611 ms en TP4), definir la lógica (joins, filtros de vigencia, agrupación, sin `ORDER BY`), las columnas exactas, el índice único `(id_categoria, mes)` para `REFRESH CONCURRENTLY`, las verificaciones de equivalencia y la política de actualización | La lógica, columnas, índice único y política de actualización de la vista materializada | Además de la medición directa prevista, en la operación quedaron registrados dos tiempos de la directa (1192.272 y 706.310 ms) que evidencian variación por caché/ejecución; se adoptó 706.310 vs. 0.284 ms como comparación más justa |
| OpenCode | Crear la Parte C en `sql/vista_materializada_tp5.sql` y `sql/test_vista_materializada_tp5.sql` | Materializar la facturación por categoría y mes con `WITH DATA`, crear el índice único sobre `(id_categoria, mes)` sin `DROP`/`IF NOT EXISTS`/`CONCURRENTLY`/extensiones, y generar consultas de lectura con `EXPLAIN ANALYZE`, dos EXCEPT de equivalencia y un bloque comentado de `REFRESH CONCURRENTLY` | La definición de la vista materializada y el índice único; las verificaciones quedaron listas para la ejecución manual | Los dos EXCEPT (A y B) resultaron en 0 filas en la validación manual, confirmando equivalencia con la consulta directa |

**Validación manual (no IA):** las mediciones se ejecutaron manualmente en DBeaver sobre `foodstore_tp3` (sin índices TP5) y `foodstore_tp5` (con índices TP5). Las mediciones de lectura corresponden a `EXPLAIN ANALYZE` real; la de escritura al mismo `INSERT` temporal de 300 detalles dentro de `BEGIN`/`ROLLBACK` en cada base. Para las vistas de la Parte B, sobre `foodstore_tp5`: las **seis verificaciones EXCEPT dieron 0 filas**, y la consulta histórica (productos eliminados con detalles vigentes) no encontró casos disponibles en la base. Para la Parte C, la consulta directa midió 1192.272 ms antes de la vista y 706.310 ms en el bloque posterior de pruebas, la lectura de la vista materializada 0.284 ms, y **ambos EXCEPT de equivalencia dieron 0 filas** sobre `foodstore_tp5`.